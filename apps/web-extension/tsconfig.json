{
    "extends": "../../tsconfig.json",
    "include": ["src", "utils"],
    "exclude": ["node_modules", "build", "dist"],
    "compilerOptions": {
        "types": ["chrome", "vite/client"],
        "baseUrl": ".",
        "rootDir": ".",
        "paths": {
            "@shared/*": ["src/shared/*"],
            "@host": ["src/host"]
          }
    }
}